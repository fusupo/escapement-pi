---
description: Show manifest status, frontier, and dispatch plan summary
---

Show the current manifest status with `manifest_status`, then show the frontier with `manifest_frontier`. If there are dispatchable items, also generate a dispatch plan with `manifest_plan`.
